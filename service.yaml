apiVersion: v1
kind: Service

metadata:
  name: backend-service
spec:
  type: NodePort
  ports:
    - protocol: 'TCP'
      port: 80
      targetPort: 8080
      nodePort: 31471
  selector:
      app: second-app